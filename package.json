{
  "name": "@datagate/mcp",
  "version": "0.1.0",
  "description": "MCP server for Datagate — dataset discovery and querying for AI agents",
  "type": "module",
  "bin": {
    "datagate-mcp": "./src/index.ts"
  },
  "scripts": {
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "datagate": "^0.1.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
